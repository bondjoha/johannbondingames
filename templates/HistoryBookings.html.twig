<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>My Bookings</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">

{% include 'headerwithoutsearch.html.twig' %}

<div class="container mx-auto p-6 max-w-3xl">

    <h1 class="text-3xl font-bold mb-6"> Bookings </h1>

    {% if bookings %}
        <div class="space-y-4">
            {% for b in bookings %}
                <div class="bg-white shadow rounded-lg p-5 border border-gray-200">
                    <h3 class="text-xl font-semibold">{{ b.Hotel_Name }}</h3>
                    <p class="text-gray-600">City: {{ b.Hotel_City_Name }}</p>
                    <p class="text-gray-600">Country: {{ b.Hotel_Country_Name }}</p>
                    <br>
                    <p><strong>Room Type:</strong> {{ b.Room_Type }}</p>
                    <p><strong>Room Number:</strong> {{ b.Room_Number }}</p>
                    <p><strong>Check-in:</strong> {{ b.Check_in|date("d-m-Y") }}</p>
                    <p><strong>Check-out:</strong> {{ b.Check_out|date("d-m-Y") }}</p>
                    <p><strong>Number of Nights:</strong> {{b.Nights}}</p>
                    <p><strong>Total Paid:</strong> €{{ b.Total_Price }}</p>

                    {% if b.Check_Out > "now"|date("d-m-Y") %}
                        <p class="text-red-500 font-semibold mt-2">Past Booking</p>
                    {% else %}
                        <p class="text-green-600 font-semibold mt-2">Upcoming Booking</p>
                    {% endif %}
                </div>
            {% endfor %}
        </div>
    {% else %}
        <p class="text-gray-500">You have no bookings yet.</p>
    {% endif %}

    <a href="index.php"
       class="mt-6 inline-block px-6 py-3 bg-yellow-400 rounded hover:bg-yellow-500 text-black font-semibold">
        Back to Home
    </a>

</div>
</body>
</html>
