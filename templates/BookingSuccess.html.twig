<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Booking Confirmed</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 text-gray-800">

{% include 'headerwithoutsearch.html.twig' %}

<div class="container mx-auto p-6 max-w-3xl">

    <h1 class="text-3xl font-bold text-green-600 mb-4">Booking Confirmed!</h1>

    <div class="bg-white shadow-lg rounded-lg p-6 mb-8">
        <h2 class="text-2xl font-semibold mb-2">{{ hotel.Hotel_Name }}</h2>
        <p class="text-gray-600">{{ hotel.Hotel_City_Name }}, {{ hotel.Hotel_Country_Name }}</p>

        <p class="mt-3"><strong>Room Type:</strong> {{ room_type }}</p>
        <p><strong>Check-in:</strong> {{ check_in|date("d-m-Y") }}</p>
        <p><strong>Check-out:</strong> {{ check_out|date("d-m-Y") }}</p>
        <p><strong>Number of Nights:</strong> {{ number_of_nights }}</p>
        <p><strong>Total Price:</strong> €{{ total_price }}</p>
    </div>    

    <a href="index.php"
       class="mt-6 inline-block px-6 py-3 bg-yellow-400 rounded hover:bg-yellow-500 text-black font-semibold">
        Back to Home
    </a>

</div>

{% include 'footer.html.twig' %}
</body>
</html>
