<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Registration - Sign Up</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">

{% include 'headerwithoutsearch.html.twig' with { 'nav_links': nav_links } %}

{% set nav_links = 
[
    { 'href': 'Index.php', 'text': 'Home' },
    { 'href': 'Login.php', 'text': 'Log In' }
] %}

{% include 'navigationmenu.html.twig' with { 'nav_links': nav_links, 'user': user } %}

  <!-- Registration Form -->
  <main class="flex justify-center items-start min-h-screen px-4 py-8">
    <div class="bg-white shadow-lg rounded-lg p-8 w-full max-w-md">
      <h2 class="text-2xl font-bold mb-6 text-center">Registration Form</h2>

      {% if message %}
        <div class="bg-blue-100 text-blue-700 px-4 py-2 rounded mb-4 text-center">
          {{ message|raw }}
        </div>
      {% endif %}

      <form method="POST" action="" class="space-y-4">
        <!-- First Name -->
        <div>
          <label for="first_name" class="block text-sm font-semibold mb-1">First Name</label>
          <input id="first_name" type="text" name="first_name" placeholder="First Name" required
                 value="{{ first_name|default('') }}"
                 class="w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-yellow-400">
        </div>

        <!-- Last Name -->
        <div>
          <label for="last_name" class="block text-sm font-semibold mb-1">Last Name</label>
          <input id="last_name" type="text" name="last_name" placeholder="Last Name" required
                 value="{{ last_name|default('') }}"
                 class="w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-yellow-400">
        </div>

        <!-- Email -->
        <div>
          <label for="user_email" class="block text-sm font-semibold mb-1">Email</label>
          <input id="user_email" type="email" name="user_email" placeholder="Enter your email" required
                 value="{{ user_email|default('') }}"
                 class="w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-yellow-400">
        </div>

        <!-- Phone Number -->
        <div>
          <label for="phone_number" class="block text-sm font-semibold mb-1">Phone Number</label>
          <input id="phone_number" type="tel" name="phone_number" placeholder="Optional"
                 value="{{ phone_number|default('') }}"
                 class="w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-yellow-400">
        </div>

       <!-- Password -->
        <div class="relative">
          <label for="user_password" class="block text-sm font-semibold mb-1">Password</label>
          <input id="password" type="password" name="user_password" required
                placeholder="Enter password"
                class="w-full px-3 py-2 border rounded text-gray-800 focus:outline-none focus:ring-2 focus:ring-yellow-400">
          
          <!-- Toggle password visibility -->
          <span id="togglePassword" class="absolute right-3 top-9 cursor-pointer text-yellow-500">👁</span>

          <!-- Password requirement warning -->
          <p id="password-warning" class="text-red-600 text-sm mt-1 hidden">
            Password must be at least 8 characters, include uppercase, lowercase, number, and special character.
          </p>
        </div>

        <!-- Confirm Password -->
        <div class="relative">
          <label for="confirm_password" class="block text-sm font-semibold mb-1">Confirm Password</label>
          <input id="confirm_password" type="password" name="confirm_password" required
                placeholder="Confirm password"
                class="w-full px-3 py-2 border rounded text-gray-800 focus:outline-none focus:ring-2 focus:ring-yellow-400">

          <p id="password-match-warning" class="text-red-600 text-sm mt-1 hidden">
            Passwords do not match.
          </p>
        </div>

        <!-- User Role -->
        <div class="relative">
          <label for="user_role" class="block text-sm font-semibold mb-1">User Role</label>
          <select id="user_role" name="user_role" required
                  class="w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-yellow-400">
            <option value="">Select role</option>
            <option value="customer" {% if user_role is defined and user_role == 'customer' %}selected{% endif %}>Customer</option>
            <option value="staff" {% if user_role is defined and user_role == 'staff' %}selected{% endif %}>Staff</option>
          </select>
        </div>

       <!-- Message -->
        <div id="staffMessage" class="mt-2 p-3 border border-yellow-400 bg-yellow-100 text-yellow-800 rounded">
          If user role is set to <strong>Staff</strong>. After registraion please contact the administrator at 
          administrator@stc.com for further steps.
        </div>

        <!-- Submit Button -->
        <button type="submit" name="submit"
                class="w-full py-2 px-4 bg-yellow-400 hover:bg-yellow-500 text-black font-semibold rounded shadow">
          Register
        </button>
      </form>
    </div>
  </main>

<script src="displaystaffmessage.js"></script>
<script src="javascripts/togglePassword.js"></script>
{% include 'footer.html.twig' %}

</body>
</html>